// Placeholder page (stub).
// NOTE: There is NO public signup. Users are provisioned by the Ops team and
// onboarded via invitation (see app/(auth)/accept-invite). Login only.
export default function LoginPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Login</h1>
      <p className="mt-2 text-muted">Coming soon.</p>
    </section>
  );
}
